<!--pages/removeFile/removeFile.wxml-->
<form bindsubmit="submitForm">
  <view>
    <block wx:for="{{items}}" wx:for-item="item">
      <input type="checkbox" value="{{item}}" bindChange="change"/>
    </block>
    <button form-type="submit" type="warn">删除选中项</button>
  </view>
</form>